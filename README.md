# Фрактальная геометрия чисел-близнецов: Анализ размерности Хаусдорфа

В этом репозитории содержатся исходный код и экспериментальный фреймворк для анализа распределения **чисел-близнецов** с помощью методов нелинейной динамики, теории хаоса и фрактальной геометрии.

Вместо классических аналитических методов теории чисел («методов сита»), этот проект моделирует Решето Эратосфена как динамический, разрушающий пространство геометрический процесс. Мы наглядно доказываем, что простые числа ведут себя как самоподобная фрактальная структура, удивительно похожая на знаменитую **Канторову пыль**.

## 🌌 Ключевой концепт: «Решето как фрактал»

В чистой математике простые числа ищут путем последовательного вычеркивания составных чисел (каждого 2-го, каждого 3-го, каждого 5-го и т.д.). Этот проект визуализирует этот процесс, сворачивая числовую ось в **полярную спираль** ($\theta = r = n$).

При таком подходе хаотичное на первый взгляд распределение простых чисел мгновенно подчиняется строгой геометрии и выстраивается в четкие радиальные рукава симметрии, образуя настоящую «числовую галактику».

Наша гипотеза утверждает: остаточное пространство **чисел-близнецов** (пар простых чисел с жестким зазором ровно в 2 единицы) обладает фрактальной плотностью, которая физически не может исчезнуть на бесконечности. Самоподобие числовой оси заставляет эти микро-зазоры дублироваться на каждом новом масштабе вселенной чисел.

## 📊 Главные открытия

* **Размерность Хаусдорфа ($D > 0$):** Скрипт использует численную регрессию методом *Box-Counting* для расчета строгой фрактальной размерности остаточного пространства близнецов.
* **Структурная устойчивость:** На масштабе до 50 000 чисел вычисленная плотность фрактала стабилизируется в районе $D \approx 0.61$. В теории хаоса любой фрактал с размерностью строго больше нуля обладает **бесконечной структурой**. Геометрия числового пространства просто не позволит парам-близнецам закончиться на бесконечности.

## 🚀 Быстрый запуск

### Системные требования

Для запуска симулятора и автоматического расчета фрактальной размерности вам понадобятся Python 3.x и библиотеки `numpy` и `matplotlib`.

```bash
pip install numpy matplotlib
```

### Запуск симулятора

Клонируйте этот репозиторий и запустите основной скрипт, чтобы построить полярную галактику чисел и запустить алгоритм подсчета коробок (Box-Counting):

```bash
git clone https://github.com
cd twin-primes-fractal
python twin_primes_fractal.py
```

## 📈 Визуальные особенности

Программа генерирует высокочеткий полярный график в неоновом стиле киберпанка:
1. **Простые числа (Бирюзовый цвет):** Фоновое распределение одиночных простых чисел (космическая пыль).
2. **Числа-близнецы (Пурпурный цвет):** Ярко выделенные пары, наглядно демонстрирующие непрерывность фрактального узора до самого края.
3. **Расчет коэффициента $D$:** Текущее значение размерности Хаусдорфа выводится в реальном времени прямо в заголовок графика.

## 📄 Научно-популярный манифест
Полный текст нашей теории и описание фрактального подхода подготовлены к публикации на Хабре, чтобы привнести методы физики хаоса в классическую теорию чисел.
